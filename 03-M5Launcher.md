# M5Launcher

## 作用

作为 Cardputer 的固件管理入口。

适合：
- 在线 OTA 安装固件；
- WebUI 上传 `.bin`；
- 切换多个固件；
- 管理分区；
- 有 SD 卡时保存固件/备份。

## 推荐设置

开启 `Always boot into Launcher`，这样每次重启先进 Launcher，再手动选择 Bruce 或其他固件。

## 常用操作

- `Enter`：选择
- `Backspace`：返回
- 从 Bruce 等独立固件回 Launcher：重启设备，在 Launcher 启动画面进入 Launcher
